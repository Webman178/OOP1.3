package transport;

public class Bus extends Transport {
    public Bus(String brand, String model, int year, String country, String colorBody, int maxSpeed, double fuelPercentage,String typeFuel) {
        super(brand, model, year, country, colorBody, maxSpeed, fuelPercentage,typeFuel);

    }
}